[project]
name = "genapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
]

[project.scripts]
genapi = "genapi:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
